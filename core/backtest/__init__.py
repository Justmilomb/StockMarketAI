"""Convenience entry-point for ``python -m core.backtest``.

Delegates to ``scripts/backtest_replay.py`` so the user can launch a
historical replay with the canonical command from the README without
remembering the script path.
"""
from __future__ import annotations
