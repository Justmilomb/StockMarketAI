# Web deploy requirements (Render license server).
#
# This file is deliberately minimal — it contains only what `server/app.py`
# and its helpers (`server/email_templates.py`, `server/email_rules.py`)
# actually import. The desktop terminal has heavier ML / UI dependencies
# and uses `requirements-desktop.txt` instead.
#
# Render build command:
#   pip install -r requirements.txt
# Render start command:
#   uvicorn server.app:app --host 0.0.0.0 --port $PORT

fastapi>=0.110.0
uvicorn>=0.27.0
pydantic>=2.5.0
psycopg2-binary>=2.9.0
requests>=2.31.0
jinja2>=3.1.0
PyJWT>=2.8.0
# Payment auth (Stripe £1 SetupIntent hold during signup) and SMS OTP
# (Twilio) for the new account-creation flow. Both clients soft-fail
# when their env vars are unset so dev / Render-without-secrets still
# boots — see `_stripe_enabled` and `_twilio_enabled` in server/app.py.
stripe>=7.0.0
twilio>=8.0.0
