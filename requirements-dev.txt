# Development, testing, and build dependencies.
# Install for local dev: pip install -r requirements-dev.txt
-r requirements.txt

# Testing
pytest>=7.0.0
pytest-mock>=3.10.0

# Building (create Windows installer)
pyinstaller>=6.0.0
